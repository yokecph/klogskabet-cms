# Add the password manually
production:
  adapter: mysql2
  encoding: utf8
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  socket: /var/run/mysqld/mysqld.sock
  database: <%= fetch(:application) %>
  username: <%= fetch(:application) %>
  password: ~
