<%= form_with(model: interval, url: (interval.new_record? ? [interval.timeline, interval] : interval), local: true) do |form| %>
  <% if interval.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(interval.errors.count, "error") %> prohibited this interval from being saved:</h2>

      <ul>
      <% interval.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title_da %>
    <%= form.text_field :title_da, id: :interval_title_da %>
  </div>

  <div class="field">
    <%= form.label :title_en %>
    <%= form.text_field :title_en, id: :interval_title_en %>
  </div>

  <div class="field">
    <%= form.label :subtitle_da %>
    <%= form.text_field :subtitle_da, id: :interval_subtitle_da %>
  </div>

  <div class="field">
    <%= form.label :subtitle_en %>
    <%= form.text_field :subtitle_en, id: :interval_subtitle_en %>
  </div>

  <div class="field">
    <%= form.label :description_da %>
    <%= form.text_area :description_da, id: :interval_description_da %>
  </div>

  <div class="field">
    <%= form.label :description_en %>
    <%= form.text_area :description_en, id: :interval_description_en %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
