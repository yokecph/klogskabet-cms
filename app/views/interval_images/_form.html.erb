<%= form_with(model: interval_image, url: (interval_image.new_record? ? [interval_image.interval, interval_image] : interval_image), local: true) do |form| %>
  <% if interval_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(interval_image.errors.count, "error") %> prohibited this interval_image from being saved:</h2>

      <ul>
      <% interval_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :file %>
    <%= form.text_field :file, id: :interval_image_file, accept: "image/jpeg,image/pjpeg,image/png" %>
  </div>

  <div class="field">
    <%= form.label :description_da %>
    <%= form.text_area :description_da, id: :interval_image_description_da %>
  </div>

  <div class="field">
    <%= form.label :description_en %>
    <%= form.text_area :description_en, id: :interval_image_description_en %>
  </div>

  <div class="field">
    <%= form.label :source_da %>
    <%= form.text_field :source_da, id: :interval_image_source_da %>
  </div>

  <div class="field">
    <%= form.label :source_en %>
    <%= form.text_field :source_en, id: :interval_image_source_en %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
